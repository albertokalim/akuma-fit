/**
 * Colores compartidos para los gráficos. Recharts no puede leer las variables
 * CSS (`var(--...)`) en todos sus props, por lo que se centralizan aquí en
 * lugar de duplicarse por vista.
 *
 * Mantener sincronizado con `src/styles/tokens.css`.
 */
export const CHART_COLORS = {
    primary: '#a78bfa',
    success: '#4ade80',
    info: '#60a5fa',
    warning: '#fbbf24',
    error: '#f87171',
};
